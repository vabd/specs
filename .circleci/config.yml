version: 2

jobs:
  build:
    docker:
      - image: cibuilds/hugo:0.53
    steps:
      - checkout
      - run:
        name: Submodules
        command: |
          git submodule init
          git submodule update --remote
      - run:
        name: Build
        command: hugo -v

